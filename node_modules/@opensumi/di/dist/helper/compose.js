"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
Object.defineProperty(exports, "__esModule", { value: true });
function dispatch(middlewareList, idx, stack, ctx) {
    if (idx <= stack.depth) {
        throw new Error('ctx.proceed() called multiple times');
    }
    stack.depth = idx;
    var maybePromise;
    if (idx < middlewareList.length) {
        var middleware = middlewareList[idx];
        maybePromise = middleware(__assign(__assign({}, ctx), { proceed: function () { return dispatch(middlewareList, idx + 1, stack, ctx); } }));
    }
    else if (ctx.proceed) {
        maybePromise = ctx.proceed();
    }
    return maybePromise;
}
function compose(middlewareList) {
    return function (ctx) {
        var stack = { depth: -1 };
        return dispatch(middlewareList, 0, stack, ctx);
    };
}
exports.default = compose;
