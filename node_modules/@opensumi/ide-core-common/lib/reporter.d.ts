import { IDisposable } from '@opensumi/ide-utils';
import { IReporterService, ReporterMetadata, IReporter, PerformanceData, PointData, IReporterTimer, REPORT_NAME } from './types/reporter';
export declare class DefaultReporter implements IReporter {
    private logger;
    performance(name: string, data: PerformanceData): void;
    point(name: string, data: PointData): void;
}
export declare class ReporterService implements IReporterService, IDisposable {
    private reporter;
    private metadata?;
    constructor(reporter: IReporter, metadata?: ReporterMetadata | undefined);
    time(name: REPORT_NAME | string): IReporterTimer;
    point(name: REPORT_NAME | string, msg?: string, extra?: any): void;
    dispose(): void;
}
//# sourceMappingURL=reporter.d.ts.map