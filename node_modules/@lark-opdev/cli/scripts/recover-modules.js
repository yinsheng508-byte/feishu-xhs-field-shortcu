const fse = require('fs-extra');
// cli打包后会生成temp_modules,在postinstall时需要通过以下脚本恢复到node_modules中

const [, , from, to] = process.argv;

fse.ensureDirSync(to);
fse.ensureDirSync(from);
fse.copySync(from, to, {
  overwrite: true,
});
fse.removeSync(from);
