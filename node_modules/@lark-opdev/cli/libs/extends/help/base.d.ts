export default class BaseHelp {
    protected highlight(text: string): string;
    protected underline(text: string): string;
    protected gray(text: string): string;
    protected bold(text: string): string;
}
