import { Command } from '@oclif/command';
import ora from 'ora';
export default abstract class extends Command {
    static examples: any[] | undefined;
    abstract run(params: any): PromiseLike<any>;
    parse(options: any, argv?: string[]): any;
    protected _version(): never;
    exit(code: number): never;
    protected notify(options: {
        title: string;
        message: string;
    }): void;
    protected success(text: string): string;
    protected catch(error: any): Promise<any>;
    protected createLoading({ text }: {
        text?: string;
    }): ora.Ora;
}
