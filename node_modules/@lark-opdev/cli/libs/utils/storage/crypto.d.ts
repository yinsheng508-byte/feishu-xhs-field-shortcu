import { CipherAbs } from '@bdeefe/feishu-devtools-core/libs/types/storage';
export declare class CryptoCipher implements CipherAbs {
    private static algorithm;
    private key;
    private iv;
    constructor({ key, iv }: {
        key?: string;
        iv?: string;
    });
    encrypt(plaintext: string): string;
    decrypt(ciphertext: string): string;
}
