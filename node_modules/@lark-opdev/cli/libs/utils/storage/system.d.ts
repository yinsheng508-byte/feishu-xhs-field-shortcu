export declare class SecretManager {
    private serviceName;
    private accountName;
    private keytar;
    available: boolean;
    private secret;
    private static createSecret;
    init(): Promise<void>;
    getSecret(): {
        iv: string;
        key: string;
    };
    clearSecret(): Promise<void>;
    private getSecretFromSystem;
    private setSecretToSystem;
}
export declare const secretManager: SecretManager;
