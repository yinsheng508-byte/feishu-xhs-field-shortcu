import { CacheType } from '../utils/enum';
import Base from './base';
export declare const config: {
    [key in Exclude<CacheType, CacheType.all>]: {
        title: string;
        task(): Promise<void>;
    };
};
export default class Cache extends Base {
    constructor();
    clear(type: CacheType, slience?: boolean): Promise<void>;
}
