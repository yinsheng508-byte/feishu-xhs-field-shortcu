import { BlockInfo, ProjectInfo } from '../types/commonData';
import { GetH5BlockPreviewTokenParameters, PreviewResourceMap } from '../types/preview';
import Base from './base';
declare type PreviewOptions = {
    path: string;
    platform: string;
    mode?: string;
    appId?: string;
    watch?: boolean;
    docsUrl?: string;
    startPage?: string;
    debug?: boolean;
    host?: string;
    enableWebviewSafeDomain?: boolean;
    useNPM?: boolean;
};
export default class Preview extends Base {
    constructor();
    gadget(options: PreviewOptions): Promise<void | {
        schemaUrl: string;
    }>;
    gadgetPlugin(options: PreviewOptions): Promise<void>;
    block(options: PreviewOptions): Promise<void>;
    readBlockInfoByPath({ projectPath, blockPath, }: {
        projectPath: string;
        blockPath: string;
    }): BlockInfo;
    readProjectConfigByPath(projectPath: string): ProjectInfo;
    getH5BlockPreviewToken(options: GetH5BlockPreviewTokenParameters): Promise<PreviewResourceMap>;
}
export {};
