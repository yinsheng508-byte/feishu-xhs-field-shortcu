import Base from './base';
declare type UploadOptions = {
    path: string;
    platform?: string;
    version: string;
    description?: string;
    useNPM?: boolean;
};
export default class Upload extends Base {
    constructor();
    gadget(options: UploadOptions): Promise<void | {
        url: string;
    }>;
    gadgetPlugin(options: UploadOptions): Promise<void | {
        url: string;
    }>;
    block(options: UploadOptions): Promise<{
        url: string;
    }>;
}
export {};
