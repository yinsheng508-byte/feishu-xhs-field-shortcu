import Base from './base';
import ModuleManager from '@bdeefe/feishu-devtools-core/libs/common/ModuleManager';
export declare const tokenCreate: (manager: ModuleManager, cidrs: string[]) => Promise<{
    error?: string;
    message: string;
    data: {
        token: string;
    };
}>;
export declare const getTokens: (manager: ModuleManager) => Promise<{
    tokens: {
        token: string;
        createTime: number;
    }[];
}>;
export declare const revokeToken: (manager: ModuleManager, token: string) => Promise<{
    error?: string;
}>;
export default class Token extends Base {
    constructor();
    create(options: {
        cidr?: string;
    }): Promise<void>;
    list(): Promise<void>;
    revoke(token: string): Promise<void>;
    checkLogin(): void;
}
