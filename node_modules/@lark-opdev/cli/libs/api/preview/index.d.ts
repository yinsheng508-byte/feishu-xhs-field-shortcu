import { GetH5BlockPreviewTokenParameters } from '../../types/preview';
import Preview from '../../implements/preview';
export default class ApiPreview {
    preview: Preview;
    constructor();
    getH5BlockPreviewToken: (options: GetH5BlockPreviewTokenParameters) => Promise<import("../../types/preview").PreviewResourceMap>;
}
