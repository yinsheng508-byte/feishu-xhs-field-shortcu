import { AccountInfo, LoginOptions, LogoutOptions } from '../../types/account';
import Account from '../../implements/account';
export default class ApiAccount {
    account: Account;
    constructor();
    login: (options: LoginOptions) => Promise<AccountInfo>;
    logout: (options: LogoutOptions) => Promise<void>;
    isLogin: (environment?: string) => boolean;
    getAccountInfo: () => AccountInfo | undefined;
}
