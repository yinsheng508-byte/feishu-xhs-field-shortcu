import { Ora } from 'ora';
import Config from '@oclif/config';
import Command from './extends/command';
export declare class Main extends Command {
    static run(argv: string[], options?: Config.LoadOptions): Promise<any>;
    init(): Promise<any>;
    run(): Promise<void>;
    protected _helpOverride(): boolean;
    protected _help(): never;
}
export declare function run(spinner: Ora, argv?: string[], options?: Config.LoadOptions): Promise<any>;
