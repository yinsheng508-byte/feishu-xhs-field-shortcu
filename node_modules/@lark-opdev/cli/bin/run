#!/usr/bin/env node
process.env.START_TIME = Date.now().toString();
process.env.CLI_ENV = 'prod';
process.env.OCLIF_TS_NODE = '0';
process.env.CURRENT_VERSION = require('../package.json').version;

require('../libs/index');
