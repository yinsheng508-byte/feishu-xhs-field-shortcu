import envConfig from './env';
export declare type EnvironmentItem = typeof envConfig.environments[0];
export declare function getEnvironmentConfig(environment: string): {
    id: string;
    name: string;
    i18n: string;
    domain: string;
    blockitDomains?: {
        internal_api: string;
        api: string;
        open: string;
        open_blockit_cdn_domain: string;
        open_blockit_cdn_domain_prefix_path: string;
    } | undefined;
    internalAPI: string;
} | undefined;
