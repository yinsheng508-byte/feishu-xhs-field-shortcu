import Manager from '../../common/ModuleManager';
import { CmdEnum, CmdMap } from '../../protocol/preview';
import { PreviewBlockOptions, PreviewH5BlockOptions, previewH5BlockReturnType } from '../../types/preview';
import PreviewModule from './live';
export default abstract class BlockPreviewModule extends PreviewModule implements CmdMap {
    protected registerEvents(manager: Manager): void;
    private getBlockPreviewFormData;
    private getBlockDocsUrl;
    private compileBlock;
    private previewChromeInstance;
    private currentCompilePath;
    private currentPreviewBlock;
    [CmdEnum.previewBlock](options: PreviewBlockOptions): Promise<void>;
    [CmdEnum.previewH5Block](options: PreviewH5BlockOptions): Promise<previewH5BlockReturnType>;
}
