import { AppType } from '../../../types/commonData';
import { StorageStruct, AppMap } from '../../../types/storage';
export declare function getProject<A extends AppType>(compilePath: string, data: StorageStruct, appType: A): AppMap[A] | void;
