declare class Encrypt {
    private iv;
    private key;
    private gadgetPublicKey;
    constructor();
    getTTCode(): string;
    decrypt(data: string): string;
    encrypt(publicKey?: string): string;
}
export default Encrypt;
