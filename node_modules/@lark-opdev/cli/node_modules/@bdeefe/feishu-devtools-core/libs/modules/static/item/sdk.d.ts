import type StaticModule from '../index';
import { RawStaticItem } from './impl';
export { JSSDKDefault } from './sdkDefault';
interface JSSDKMeta {
    larkVersion: string;
}
export declare class JSSDKItem implements RawStaticItem<JSSDKMeta> {
    type: 'remote' | 'local';
    uri: string;
    resourceType: "JSSDK";
    subType: "";
    targetRelPath: string;
    name: string;
    version: string;
    resourceName: string;
    unzipName: string;
    delPath: string;
    meta: JSSDKMeta;
    constructor(type: 'remote' | 'local', uri: string, name: string, version: string, larkVersion: string);
    getMeta(_: string): JSSDKMeta;
    finish(this: StaticModule, itemPath: string): void;
}
