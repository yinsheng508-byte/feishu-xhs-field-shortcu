import { GeneratorPlugin } from './plugins/type';
import { Commands, ModuleName } from '../../../protocol';
import { Deps } from '../../../protocol/typeUtil';
export interface GeneratorState {
    [key: string]: any;
}
export { TemplateConfig } from '../../../utils/update/type';
export { CmdEnum as UICmdEnum, CmdMap as UI } from '../../../protocol/ui';
export declare type Execute = Commands<Deps<ModuleName.creator>>;
export interface GeneratorConfig {
    plugins: GeneratorPlugin[];
}
export { Lang } from '../../../types/commonData';
