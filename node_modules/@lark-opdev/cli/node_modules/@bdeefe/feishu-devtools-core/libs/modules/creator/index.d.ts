import BaseModule from '../../common/Module';
import { CmdEnum, CmdMap, EventMap, GenerateV2Options } from '../../protocol/creator';
import { AppType } from '../../types/commonData';
import { ModuleName } from '../../types/protocol';
import { StaticStorageItem } from '../static';
export default class CreatorModule extends BaseModule<ModuleName.creator, EventMap> implements CmdMap {
    constructor();
    localPath: string;
    [CmdEnum.list](options: {
        appType: AppType;
    }): Promise<StaticStorageItem[] | undefined>;
    [CmdEnum.generate](options: {
        name: string;
        target: string;
        meta?: {
            appId: string;
            projectName: string;
            blockTypeId?: string;
        };
    }): Promise<boolean>;
    [CmdEnum.generateV2](options: GenerateV2Options): Promise<void>;
}
