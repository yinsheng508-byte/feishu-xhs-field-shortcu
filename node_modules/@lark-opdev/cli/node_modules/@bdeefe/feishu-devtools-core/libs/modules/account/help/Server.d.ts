import { LoginOptions } from '../../../types/account';
declare type Option = Pick<LoginOptions, 'showSuccessPage'> & {
    language: string;
};
export default class Server {
    private instance;
    private url;
    private options;
    private listeners;
    private onErrorListener;
    private terminator;
    private timeout;
    constructor(options: Option);
    private getTemplate;
    private getCurrentTemplateInfo;
    start(): Promise<string>;
    close(): void;
    set loginUrl(url: string);
    set isTimeout(value: true);
    onSuccess(callback: Function): void;
    onError(callback: Function): void;
}
export {};
