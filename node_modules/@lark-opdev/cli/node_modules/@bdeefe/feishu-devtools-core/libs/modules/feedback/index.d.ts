import BaseModule from '../../common/Module';
import { CmdEnum, EventMap, CmdMap } from '../../protocol/feedback';
import { ModuleName } from '../../types/protocol';
import { DocUrl } from '../../types/commonData';
export default class FeedbackModule extends BaseModule<ModuleName.feedback, EventMap> implements CmdMap {
    ctx: {
        version: string;
        commitHash: string;
    };
    constructor(ctx: {
        version: string;
        commitHash: string;
    });
    [CmdEnum.Feedback](): string;
    [CmdEnum.DocUrl](): DocUrl[];
    [CmdEnum.FeedbackTip](): string;
    [CmdEnum.reportIssue](params: {
        types: string[];
        time: string;
        desc: string;
        pics: string[];
        contact: string;
        useLog: boolean;
    }): Promise<{
        flag: boolean;
        message?: string;
    }>;
}
