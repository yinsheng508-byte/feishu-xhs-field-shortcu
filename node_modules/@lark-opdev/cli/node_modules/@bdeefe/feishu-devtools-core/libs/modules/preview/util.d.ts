import type { AppType } from '../../types/commonData';
import { Deps } from 'packages/refactor/protocol/typeUtil';
import { Commands, ModuleName } from '../../protocol';
export declare const PCModeMap: {
    sidebar: string;
    appcenter: string;
    window: string;
    "window-semi": string;
};
export declare const getDebugInfo: (execute: Commands<Deps<ModuleName.preview>> | Commands<Deps<ModuleName.debugger>>, compilePath: string, appType: AppType) => {
    debug_info: {
        user_id: string;
        tenant_id: string;
    };
} | {
    debug_info?: undefined;
};
