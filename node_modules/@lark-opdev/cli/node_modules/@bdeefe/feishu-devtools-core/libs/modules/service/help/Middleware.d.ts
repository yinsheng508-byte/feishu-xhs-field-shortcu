import type { Options } from './Request';
import { InternalAxiosRequestConfig as AxiosRequest, AxiosResponse } from 'axios';
export declare enum ServerResponseCode {
    SUCCESS = 0,
    SESSION_EXPIRE_CODE = 34,
    PASSPORT_SESSION_EXPIRE_CODE = 4401,
    NOT_LOGIN = 4,
    COMPILE_ERROR = 2200
}
declare class Middleware {
    static urlParams(request: AxiosRequest): AxiosRequest;
    static queryParams(request: AxiosRequest): AxiosRequest;
    static isSessionExpired(options: Options): (response: AxiosResponse) => AxiosResponse;
    static formatResponse(options: Options): (response: AxiosResponse) => AxiosResponse;
    static logger: {
        request(logger: any): (request: AxiosRequest) => AxiosRequest;
        response(logger: any): (response: AxiosResponse) => AxiosResponse;
    };
    static parseCookie(response: AxiosResponse): AxiosResponse;
}
export default Middleware;
