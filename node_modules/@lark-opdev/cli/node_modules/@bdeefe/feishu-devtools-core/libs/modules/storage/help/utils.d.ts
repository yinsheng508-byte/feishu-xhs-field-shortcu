import { DiffPaths, ProjectStorage } from '../../../types/storage';
export declare function getPathId(path: string): string;
interface Diff {
    <T>(now: T, old: T | void): DiffPaths;
    <T>(now: void | T, old: T): DiffPaths;
}
export declare const diff: Diff;
export declare function getTmpConfig(base: string): ProjectStorage['tmpConfig'];
export {};
