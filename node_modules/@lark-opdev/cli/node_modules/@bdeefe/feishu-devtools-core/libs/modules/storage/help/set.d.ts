import { StorageStruct, DiffPaths, AppMap } from '../../../types/storage';
import Storage from '../index';
import { AppType } from '../../../types/commonData';
export declare function setRoot<N extends Exclude<keyof StorageStruct, 'projects'>>(key: N, data: StorageStruct[N] | void, all: StorageStruct, storage: Storage, appType: AppType): DiffPaths;
export declare function setProject<A extends AppType>(compilePath: string, data: AppMap[A] | void, all: StorageStruct, storage: Storage, appType: A): DiffPaths;
