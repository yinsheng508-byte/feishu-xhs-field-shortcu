import { AppType } from '../../../types/commonData';
export declare type ResourceType = AppType | 'JSSDK' | string;
export interface RawStaticItem<T = {}> {
    type: 'remote' | 'local';
    uri: string;
    resourceType: ResourceType;
    subType: '' | 'plugin';
    targetRelPath: string;
    name: string;
    version: string;
    unzipName: string;
    delPath: string;
    resourceName: string;
    meta: T;
    getMeta: (target: string) => T;
    finish: (itemPath: string) => void;
}
