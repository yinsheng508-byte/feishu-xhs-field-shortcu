import { CompileEndResult } from '../../../protocol/compile';
import Compile from '../compile';
declare class GadgetCompile extends Compile {
    getPackVersion(): string;
    start(): Promise<CompileEndResult>;
    get events(): {
        compile: string;
        first: string;
        watch: string;
    } | {
        compile: string;
        first: string;
        watch: string;
    } | {
        compile: string;
        first: string;
        watch: string;
    } | {
        compile: string;
        first: string;
        watch: string;
    };
}
export default GadgetCompile;
