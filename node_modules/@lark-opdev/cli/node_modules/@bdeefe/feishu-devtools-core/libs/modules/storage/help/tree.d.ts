import { Paths, DiffPaths } from '../../../types/storage';
export default class Tree {
    private handlers;
    private children;
    addListener(fn: () => void, paths?: Paths): () => void;
    private remove;
    removeListener(fn: () => void, paths?: Paths): boolean;
    private run;
    private _emit;
    emit(diffPath: DiffPaths): void;
    private static hasRun;
}
