import { ProjectInfo, BlockConfig } from './commonData';
export interface User {
    id: string;
    name: string;
    avatar: string;
}
export interface BlockStartData {
    httpServerPort: number;
    blockTypeID: string;
    projectConfig: ProjectInfo;
    allBlockConfig: BlockConfig[];
    blockIdx: number;
}
export interface BlockEnvData {
    larkSession?: string;
    environment: 'online' | 'boe';
    region: 'feishu' | 'lark';
    domain: string;
    user?: User;
    tenant?: User;
}
