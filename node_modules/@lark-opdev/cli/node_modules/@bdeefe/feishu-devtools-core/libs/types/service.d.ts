export interface ProxyConfig {
    type: 'direct' | 'system' | 'manual';
    proxy: {
        protocol?: string;
        host?: string;
        port?: number;
        auth?: {
            username: string;
            password: string;
        };
    };
}
