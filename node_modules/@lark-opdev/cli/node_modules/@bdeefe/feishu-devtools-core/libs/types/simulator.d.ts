import { CommonOptions } from './index';
import { AppType } from './commonData';
export interface StartOptions extends CommonOptions {
    compilePath?: string;
    from: 'simulator' | 'devtools';
    appType: AppType;
}
export declare enum CompilePathType {
    sourceCode = 1,
    pkgFile = 2,
    compiledCode = 3,
    unknown = 0
}
