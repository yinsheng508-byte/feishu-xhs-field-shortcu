import { AbsStorageUtils, StorageStruct } from '../types/storage';
import StorageUtilsV2 from './storageV2';
export default class StorageUtils implements AbsStorageUtils {
    static instance: StorageUtils;
    private rootPath;
    private file;
    constructor();
    get<K extends keyof StorageStruct>(key: K): StorageStruct[K];
    set<K extends keyof StorageStruct>(key: K, data: StorageStruct[K] | void): void;
    getRootPath(): string;
    refreshPath(): void;
}
export declare const getStorageIns: () => StorageUtilsV2 | StorageUtils;
