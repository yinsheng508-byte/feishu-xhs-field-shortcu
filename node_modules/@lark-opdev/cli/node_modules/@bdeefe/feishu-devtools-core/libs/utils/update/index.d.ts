import Manager from '../../common/ModuleManager';
import Request from '../../modules/service/help/Request';
import { ProxyConfig } from '../../types/service';
import { FGCacheFileType, UpdateData } from './type';
export default class Update {
    request: Request;
    constructor();
    getTenantUserId(): Promise<{
        userId: string;
        tenantId: string;
        environment: string;
    }>;
    getStorageProxy(): ProxyConfig;
    getUpdateUrl(version: string): Promise<string>;
    getFGUrl(version: string): Promise<string>;
    fetchUpdateData(version: string): Promise<UpdateData | null>;
    writeUpdateData(manager?: Manager, needUpdate?: boolean): Promise<UpdateData | null>;
    getFGData(): FGCacheFileType;
    getUpdateData(): UpdateData;
    writeFGData(): Promise<{} | void>;
    private updateStaticResource;
}
