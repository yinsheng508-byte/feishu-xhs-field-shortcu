import type { CipherConfig } from './cipher';
import { AbsStorageUtils, StorageStruct } from '../../types/storage';
export interface StorageUtilsConfig {
    cipherConfig: CipherConfig;
}
export default class StorageUtilsV2 implements AbsStorageUtils {
    static instance: StorageUtilsV2;
    private rootPath;
    private file;
    private cipher;
    constructor(config: StorageUtilsConfig);
    get<K extends keyof StorageStruct>(key: K): StorageStruct[K];
    set<K extends keyof StorageStruct>(key: K, data: StorageStruct[K] | void): void;
    getRootPath(): string;
    refreshPath(): void;
    private transformLegacyData;
}
