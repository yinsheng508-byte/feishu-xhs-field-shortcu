import { IStorageSecJsonFile } from '../../types/storage';
import { CipherAbs } from '../../types/storage';
export interface CipherConfig {
    cryptoCipherIns: CipherAbs;
    systemCipherIns?: CipherAbs;
}
export declare class Cipher {
    private cryptoCipher;
    private systemCipher?;
    constructor(options: CipherConfig);
    decrypt(json: IStorageSecJsonFile): string;
    encrypt(content: string): IStorageSecJsonFile;
}
