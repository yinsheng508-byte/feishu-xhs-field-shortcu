import { CoreErrorCode } from './Error';
export declare class RequestError extends Error {
    type: CoreErrorCode;
    logId: string;
    code: number;
    stable?: 0 | 1;
    advise?: string;
    constructor(message: string, code: number, logId: string, stable?: 0 | 1, advise?: string);
    IPCFormat(): {
        type: CoreErrorCode;
        message: string;
        code: number;
        logId: string;
        advise: string;
        stack: string | undefined;
        stable: 0 | 1 | undefined;
    };
}
export declare function isRequestError(err: unknown): err is RequestError;
