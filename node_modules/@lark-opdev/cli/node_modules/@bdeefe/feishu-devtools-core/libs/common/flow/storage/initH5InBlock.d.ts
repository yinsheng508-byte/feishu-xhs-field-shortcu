import { H5InBlockStorage, StorageStruct } from '../../../types/storage';
export default function initH5InBlock(compilePath: string, basePath: string, data: StorageStruct, _environment: string): Promise<H5InBlockStorage>;
