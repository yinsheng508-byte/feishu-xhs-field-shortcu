export default class Task {
    private hasInit;
    private target;
    private task;
    private readonly debug;
    constructor(target: HTMLIFrameElement, debug?: boolean);
    private clearTask;
    private send;
    postMessge(data: unknown): void;
    static message: string;
}
