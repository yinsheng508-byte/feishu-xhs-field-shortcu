import { ProjectStorage, StorageStruct } from '../../../types/storage';
export default function initProject(compilePath: string, basePath: string, data: StorageStruct, _environment: string): Promise<ProjectStorage>;
