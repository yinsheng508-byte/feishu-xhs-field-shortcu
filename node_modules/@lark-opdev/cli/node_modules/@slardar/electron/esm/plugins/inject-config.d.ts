import { ElectronReportEvent, ElectronConfig, ElectronClient } from '../types';
export declare const addConfigToReportEvent: (ev: ElectronReportEvent, config: ElectronConfig) => ElectronReportEvent;
export declare const InjectConfigPlugin: (client: ElectronClient) => void;
