import { CrashReporter } from 'electron';
import { ContextAgent } from '@slardar/sdk-template';
import { ElectronClient } from '../../types';
export declare const CRASH_PLUGIN_NAME = "crash";
export declare const CRASH_EV_TYPE = "crash";
export interface CrashConfig {
    crashReporter: CrashReporter | null;
}
export interface CrashMonitorPluginConfig {
    [CRASH_PLUGIN_NAME]: CrashConfig;
}
export declare const defaultConfig: CrashConfig;
export declare type WithCaptureCrash<C extends ElectronClient> = C;
export declare function CrashMonitorPlugin<C extends ElectronClient>(client: C): void;
/** 创建一个通知 context 更新的 listener */
export declare function createContextListener(context: ContextAgent, onUpdate: () => void): {
    restoreFns: ((strict?: boolean | undefined) => void)[];
};
