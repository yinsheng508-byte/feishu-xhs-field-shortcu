import { JsError } from '@slardar/sdk-web';
export declare const dedupe: () => (currentError: Error | undefined) => Error | undefined;
export declare const normalize: (ex: any) => JsError | undefined;
