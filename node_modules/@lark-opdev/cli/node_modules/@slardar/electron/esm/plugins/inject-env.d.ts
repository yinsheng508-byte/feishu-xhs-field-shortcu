import { CommonContext } from '@slardar/sdk-web';
import { ElectronClient, ElectronReportEvent } from '../types';
export declare const addEnvToSendEvent: (ev: ElectronReportEvent) => ElectronReportEvent & {
    extra: Partial<CommonContext> & {
        timestamp: number;
        sdk_version: string;
        sdk_name: string;
    };
};
export declare const InjectEnvPlugin: (client: ElectronClient) => void;
