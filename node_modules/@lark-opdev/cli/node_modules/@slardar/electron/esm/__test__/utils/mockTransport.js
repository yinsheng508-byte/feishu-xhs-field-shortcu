import sinon from 'sinon';
export var mockTransport = function (options) {
    var _options = options;
    var setOptions = function (options) {
        _options = options;
    };
    var fakeFunc = function (_a) {
        var success = _a.success, fail = _a.fail;
        if (!_options) {
            return;
        }
        if (_options.isSuccess && success) {
            success(_options.returnData);
        }
        else if (fail) {
            fail(_options.returnData);
        }
    };
    var get = sinon.fake(fakeFunc);
    var post = sinon.fake(fakeFunc);
    var transport = {
        get: get,
        post: post,
    };
    return { transport: transport, get: get, post: post, setOptions: setOptions };
};
//# sourceMappingURL=mockTransport.js.map