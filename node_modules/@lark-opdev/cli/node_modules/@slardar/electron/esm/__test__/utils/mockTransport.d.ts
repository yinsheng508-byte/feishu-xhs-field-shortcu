import sinon from 'sinon';
import { ElectronTransport } from '../../types/transport';
declare type Options = {
    isSuccess: boolean;
    returnData?: any;
};
export declare const mockTransport: (options?: Options | undefined) => {
    transport: ElectronTransport;
    get: sinon.SinonSpy<any[], any>;
    post: sinon.SinonSpy<any[], any>;
    setOptions: (options: Options) => void;
};
export {};
