import { WithSubject } from '@slardar/sdk-template';
import { ActionMonitorConfig, StartAction } from '../../monitors/action';
import { WebClient } from '../../types';
export declare const ACTION_MONITOR_PLUGIN_NAME = "action";
export { ActionMonitorConfig } from '../../monitors/action';
export declare type WithAction<C extends WebClient> = C & {
    startAction?: StartAction;
};
export interface ActionMonitorPluginConfig {
    [ACTION_MONITOR_PLUGIN_NAME]?: boolean | Partial<ActionMonitorConfig>;
}
export declare function ActionMonitorBasePlugin<C extends WithSubject<WebClient>>(client: C, config?: ActionMonitorConfig): void;
export declare function ActionMonitorPlugin<C extends WithSubject<WebClient>>(client: C): void;
