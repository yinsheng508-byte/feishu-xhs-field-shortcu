import { WithSubject } from '@slardar/sdk-template';
import { PageviewProps, SendPageview } from '../../monitors/pageview/type';
import { WebClient } from '../../types';
export declare const PAGEVIEW_MONITOR_PLUGIN_NAME = "pageview";
export declare type WithSendPageview<C extends WithSubject<WebClient>> = C & {
    sendPageview?: SendPageview;
};
export declare type PageviewMonitorConfig = Omit<PageviewProps, 'onPidUpdate' | 'initPid'>;
export interface PageviewMonitorPluginConfig {
    [PAGEVIEW_MONITOR_PLUGIN_NAME]?: Partial<PageviewMonitorConfig> | boolean;
}
export declare function PageviewMonitorPlugin<C extends WithSubject<WebClient>>(client: C): void;
