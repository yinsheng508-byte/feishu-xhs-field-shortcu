import { WithSubject, WithContext } from '@slardar/sdk-template';
import { AjaxMonitorConfig, WrapXhr } from '../../monitors/http/type';
import { HttpReport, ReportToReportEvent, WebClient, WebVolClient } from '../../types';
export declare const AJAX_MONITOR_PLUGIN_NAME = "ajax";
export declare type WithWrapXhr<C extends WithSubject<WebClient>> = C & {
    wrapXhr?: WrapXhr;
};
export interface AjaxMonitorPluginConfig {
    [AJAX_MONITOR_PLUGIN_NAME]?: Partial<AjaxMonitorConfig> | boolean;
}
export declare const getCbHook: (client: WithContext<WebClient>) => (cb?: ((ev: ReportToReportEvent<HttpReport>) => void) | undefined) => ((ev: ReportToReportEvent<HttpReport>) => void) | undefined;
export declare function AjaxMonitorPlugin<C extends WithContext<WithSubject<WebClient>>>(client: C): void;
export declare function AjaxMonitorVolPlugin<C extends WithSubject<WebVolClient>>(client: C): void;
