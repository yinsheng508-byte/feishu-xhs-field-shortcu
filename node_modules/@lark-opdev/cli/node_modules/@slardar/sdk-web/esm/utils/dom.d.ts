/**
 * from sentry https://github.com/getsentry/sentry-javascript/blob/5.30.0/packages/utils/src/browser.ts
 */
import { Breadcrumb } from '../types';
export declare const UNKNOWN_PATH = "<unknown>";
/**
 * Given a child DOM element, returns a query-selector statement describing that
 * and its ancestors
 * e.g. [HTMLElement] => body > div > input#foo.btn[name=baz]
 * @returns generated DOM path
 */
export declare function htmlTreeAsString(elem: unknown): string;
declare type InstrumentHandlerType = 'dom';
export declare const applyDomAndKeyPress: (debounceDuration: number) => readonly [(name: string, handler: (params: {
    event: Event;
    name: string;
}) => void) => (event: Event) => void, (handler: (params: {
    event: Event;
    name: string;
}) => void) => (event: Event) => void];
export declare const triggerHandlers: (domBreadcrumb: (handlerData: {
    event: Event;
    name: string;
}) => void, type: InstrumentHandlerType) => (data: {
    event: Event;
    name: string;
}) => void;
/**
 * Creates breadcrumbs from DOM API calls
 */
export declare const domBreadcrumb: (addBreadcrumb: (bd: Breadcrumb) => void) => (handlerData: {
    event: Event;
    name: string;
}) => void;
export {};
