import { createBaseConfigManager, createClient, directBuilder, id, noopSender, SubjectPlugin, } from '@slardar/sdk-template';
import { now } from '../date';
export var createTestWebClient = function () {
    var client = createClient({
        builder: directBuilder,
        createSender: function () { return noopSender; },
        createDefaultConfig: function () { return ({
            pid: '',
            viewId: '_' + now(),
            userId: '',
            deviceId: '',
            sessionId: '',
            plugins: {},
            sample: {
                sample_rate: 1,
                sample_granularity: 'session',
                include_users: [],
                rules: {},
            },
        }); },
        userConfigNormalizer: id,
        initConfigNormalizer: id,
        createConfigManager: createBaseConfigManager,
        validateInitConfig: function () { return true; },
    });
    SubjectPlugin(client, undefined);
    return client;
};
//# sourceMappingURL=util.js.map