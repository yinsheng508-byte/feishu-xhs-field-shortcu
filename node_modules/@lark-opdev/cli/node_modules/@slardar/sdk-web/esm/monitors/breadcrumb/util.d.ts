import { Processor } from '@slardar/sdk-template';
import { Breadcrumb } from '../../types';
import { applyDomAndKeyPress, domBreadcrumb, triggerHandlers } from '../../utils';
import { OnMaxBreadcrumbs, PreBreadcrumb } from './type';
export { applyDomAndKeyPress, domBreadcrumb, triggerHandlers };
export declare const applyBreadcrumb: (maxBreadcrumbs?: number, onAddBreadcrumb?: Processor<PreBreadcrumb>, onMaxBreadcrumbs?: OnMaxBreadcrumbs) => readonly [() => Breadcrumb[], (breadcrumb: PreBreadcrumb) => void];
