import { MonitorConstructor } from '../../../types';
import { PerformanceReport } from '../../../types/report';
export interface IItem {
    score: number;
    time: number;
}
export interface FMPMonitorConfig {
    renderType?: 'CSR' | 'SSR';
}
export declare type tirggerFMP = () => void;
export declare type FMPMonitorConstructor = MonitorConstructor<FMPMonitorConfig, PerformanceReport, [
    document: Document,
    MutationObserver: typeof MutationObserver,
    navigationStart: number,
    Raf: Window['requestAnimationFrame'],
    Caf: Window['cancelAnimationFrame']
], [
    tirggerFMP
]>;
