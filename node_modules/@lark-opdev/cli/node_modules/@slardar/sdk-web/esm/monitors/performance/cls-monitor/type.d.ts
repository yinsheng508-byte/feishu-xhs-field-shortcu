import { MonitorConstructor } from '@slardar/types';
import { PerformancePayload } from '../../../types/thrift/browser/collector_v2/performance';
import { PerformancePrecollect } from '../type';
export interface LayoutShift extends PerformanceEntry {
    value: number;
    hadRecentInput: boolean;
}
export declare type CLSMonitorConstructor = MonitorConstructor<[
    precollect: PerformancePrecollect
], PerformancePayload, [
    PerformanceObserver: typeof PerformanceObserver
], [
    CountAndClear: () => void,
    Teardown: () => void
]>;
