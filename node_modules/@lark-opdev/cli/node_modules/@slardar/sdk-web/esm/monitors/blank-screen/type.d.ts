import { BlankReport, MonitorConstructor, WebReportEvent } from '../../types';
import { BlankRelatedError } from '../../types/thrift/browser/collector_v2/blank_screen';
export interface TearDown {
    (): void;
}
export declare type BlankRelatedErrorInfo = BlankRelatedError & {
    timestamp: number;
};
export interface BlankScreenMonitorConfig {
    autoDetect?: boolean;
    threshold: number;
    screenshot: boolean;
    rootSelector?: string;
    ssUrl: string;
    mask: boolean;
    partialShot: boolean;
    /**
     * 0 ~ 1,default to 0.1
     */
    quality: number;
}
export interface DetectBlankScreen {
    (): void;
}
export interface ApplyOtherReport {
    (ev: WebReportEvent): void;
}
export declare type BlankScreenMonitorConstructor = MonitorConstructor<BlankScreenMonitorConfig, BlankReport, [
    win: Window,
    document: Document,
    PerformanceObserver: typeof PerformanceObserver,
    MutationObserver: typeof MutationObserver,
    performance: Performance
], [
    TearDown,
    ApplyOtherReport,
    DetectBlankScreen
]>;
