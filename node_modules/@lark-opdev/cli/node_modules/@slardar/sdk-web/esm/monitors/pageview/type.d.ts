export declare const enum PageviewSourceType {
    init = "init",
    hash = "hash",
    history = "history",
    user_set = "user_set"
}
export interface SendPageview {
    (pid: string): void;
}
export interface PidExtractor {
    (url: string): string;
}
export interface PageviewProps {
    sendInit: boolean;
    routeMode: 'history' | 'hash' | 'manual';
    initPid?: string;
    extractPid?: PidExtractor;
    onPidUpdate?: (pid: string) => void;
}
