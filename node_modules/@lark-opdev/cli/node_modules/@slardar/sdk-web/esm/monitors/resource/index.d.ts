import { SafeObservable } from '@slardar/sdk-template';
import { LoadNext } from '../../collector/page';
import { ResourceNext } from '../../collector/pref';
import { WebReportEvent } from '../../types';
export interface ResourceProps {
    ignoreUrls: (string | RegExp)[];
    slowSessionThreshold: number;
    ignoreTypes?: string[];
}
declare type ResourceGetterWithContext = (report: (ev: WebReportEvent) => void, tearDownGroup: (() => void)[], context: [SafeObservable<LoadNext>, () => SafeObservable<ResourceNext>], config: ResourceProps) => void;
export declare const resourceGetterWithContext: ResourceGetterWithContext;
export {};
