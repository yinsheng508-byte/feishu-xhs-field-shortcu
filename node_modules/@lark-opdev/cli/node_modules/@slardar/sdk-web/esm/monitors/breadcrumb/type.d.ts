import { Processor } from '@slardar/sdk-template';
import { Breadcrumb, MonitorConstructor } from '../../types';
export declare type PreBreadcrumb = Omit<Breadcrumb, 'timestamp'> & Partial<Pick<Breadcrumb, 'timestamp'>>;
export interface BreadcrumbProps {
    maxBreadcrumbs?: number;
    onAddBreadcrumb?: Processor<PreBreadcrumb>;
    onMaxBreadcrumbs?: OnMaxBreadcrumbs;
    dom?: boolean;
}
export interface TearDown {
    (): void;
}
export declare type OnMaxBreadcrumbs = (breadcrumbs: Breadcrumb[], maxBreadcrumbs: number) => Breadcrumb[];
export declare type GetBreadcrumbs = () => Breadcrumb[];
export declare type AddBreadcrumb = (b: PreBreadcrumb) => void;
export declare type BreadcrumbMonitorConstructor = MonitorConstructor<BreadcrumbProps, any, [
    document: Document
], [
    GetBreadcrumbs,
    AddBreadcrumb,
    TearDown
]>;
