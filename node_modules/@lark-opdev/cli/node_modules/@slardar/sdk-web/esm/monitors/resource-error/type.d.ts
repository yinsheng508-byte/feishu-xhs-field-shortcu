export interface ResourceErrorData {
    url: string;
    tagName: string;
    xpath?: string;
}
export interface ReportResourceError {
    (data: ResourceErrorData): void;
}
export declare type ResourceErrorProps = {
    ignoreUrls: (string | RegExp)[];
    includeUrls: (string | RegExp)[];
    dedupe: boolean;
};
