import { Observer } from '@slardar/sdk-template';
import { WebClient } from '../types';
import { Common } from '../types/thrift/browser/collector_v2/common';
export declare const PV_SUBJECT_NAME = "view_0";
export declare type PVNext = (args: Partial<Common>) => void;
/**
 * 由于跟当前实例强相关 所以不能挂在global上
 */
export declare const applyPV: (client: WebClient) => Observer<PVNext>;
export declare const FIRST_PV_SUBJECT_NAME = "f_view_0";
/**
 * 由于跟当前实例强相关 所以不能挂在global上
 */
export declare const applyFirstPV: (client: WebClient) => Observer<PVNext>;
