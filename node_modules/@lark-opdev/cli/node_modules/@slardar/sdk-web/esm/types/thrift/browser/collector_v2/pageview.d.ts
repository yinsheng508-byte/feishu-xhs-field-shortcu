import * as common from './common';
export { common };
export interface PageviewSend {
    /** pageview */
    ev_type: string;
    payload: PageviewPayload;
    common: common.Common;
}
export interface PageviewPayload {
    pid: string;
    /** 触发原因 init | path change | hash change | history state change | user-set */
    source: string;
}
