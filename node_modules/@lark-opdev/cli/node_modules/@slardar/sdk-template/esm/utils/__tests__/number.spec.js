import test from 'ava';
import { isHitBySampleRate } from '../number';
test('number - isHitBySampleRate', function (t) {
    t.assert(isHitBySampleRate(1));
    t.assert(isHitBySampleRate(1));
    t.assert(isHitBySampleRate(1));
    t.false(isHitBySampleRate(0));
    t.false(isHitBySampleRate(0));
    t.false(isHitBySampleRate(0));
});
//# sourceMappingURL=number.spec.js.map