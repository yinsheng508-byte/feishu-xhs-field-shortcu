import { createLogger } from '../utils';
export var LOGGER_NAME = 'logger';
var logFn = createLogger('[LoggerPlugin]');
export function LoggerPlugin(client) {
    client.on('send', function (ev) {
    });
}
//# sourceMappingURL=logger.js.map