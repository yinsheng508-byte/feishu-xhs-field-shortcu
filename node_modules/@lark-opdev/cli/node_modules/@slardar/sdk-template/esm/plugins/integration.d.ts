import { AnyClient, Client } from '../types';
export interface Integration<T extends AnyClient> {
    name: string;
    setup: (client: T) => void;
    tearDown?: () => void;
}
export interface ConfigWithIntegration<T extends AnyClient> {
    integrations?: Integration<T>[];
}
export declare function IntegrationPlugin<E extends ConfigWithIntegration<any>, T extends ConfigWithIntegration<any>>(client: Client<any, any, any, any, E, any, T>): void;
