import { Config } from 'bili'

const config: Config = {
  input: 'src/index.ts',
  output: {
    format: ['cjs', 'esm', 'umd', 'umd-min'],
    moduleName: 'starlingIntl',
    sourceMap: false
  },
  plugins: {
    copy2: {
      assets: [['src/index.d.ts', 'index.d.ts']]
    }
  }
}

export default config
