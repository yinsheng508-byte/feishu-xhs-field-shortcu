const PREFIX = 'Starling_intl:'
const logger = {
  warn(...args) {
    const prefix = `${PREFIX} [WARNING]`
    console.warn(prefix, ...args)
  },
  error(...args) {
    const prefix = `${PREFIX} [ERROR]`
    console.error(prefix, ...args)
  }
}

export default logger
