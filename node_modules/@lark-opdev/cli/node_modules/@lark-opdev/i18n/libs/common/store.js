"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.I18nStore = void 0;
const eventemitter3_1 = __importDefault(require("eventemitter3"));
// TODO 后续使用统一的 storage 模块。暂时是内存态通过静态方法来管理
class I18nStore extends eventemitter3_1.default {
    setLang(lang) {
        I18nStore.lang = lang;
        this.emit('langChange', lang);
        // 这么设计是因为，后续 i18nStore set 可能是一个异步逻辑
        return Promise.resolve(lang);
    }
    getLang() {
        return I18nStore.lang;
    }
    onLangChange(fn) {
        this.on('langChange', fn);
    }
}
exports.I18nStore = I18nStore;
I18nStore.lang = 'en-US';
