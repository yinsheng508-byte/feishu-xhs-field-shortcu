import { IIntlInitOptions } from '@ies/starling_intl';
import { I18nBase, Lang } from '../common/index';
export * from '../common/index';
export declare class I18nNode extends I18nBase {
    constructor(...args: ConstructorParameters<typeof I18nBase>);
    static init({ isOversea, opt, }: {
        isOversea?: Boolean;
        opt?: IIntlInitOptions;
    }): I18nNode;
    setLang(lang: Lang): Promise<Lang>;
    onLangChange(fn: (lang: Lang) => void): void;
}
declare let I18n: I18nNode;
export default I18n;
